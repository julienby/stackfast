Crée une nouvelle app : $ARGUMENTS (attendu : `<stack> <slug>`)

1. Lance `bin/new-app <stack> <slug>`. Montre la sortie (dont le bloc proxy). Slug refusé → arrête-toi.
2. Lance `bin/check <stack> <slug>` : doit être vert sur le gabarit. Cite la sortie.
3. Ouvre `apps/<stack>/<slug>/PRD.md` : pose-moi les questions des sections vides, une par une. Ne remplis rien en silence.
4. Une fois le PRD rempli, suis « Première session » de l'`AGENTS.md` de l'app. Aucune feature avant.
