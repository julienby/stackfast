Rétrospective de la tâche qui vient de se terminer.

Réponds en 5 lignes max :
1. Ai-je été corrigé, ou ai-je dû revenir sur quelque chose ? Si oui, quoi précisément.
2. Une règle d'une ligne qui l'aurait évité, au format `- YYYY-MM-DD — règle. (erreur observée : ...)`. Si aucune erreur : « rien à ajouter », et on passe.
3. À quel niveau ? **app** (`apps/<stack>/<slug>/AGENTS.md`), **instance** (`AGENTS.md`), **stack** (hub `ma-stack-ia`, `INIT/stacks/<stack>/AGENTS.md`, puis `install.sh` relancé), **global** (`~/.claude/CLAUDE.md`). Une règle qui vaut pour deux apps remonte en stack ; pour deux stacks, en global.
4. Une règle existante qui n'a servi à rien cette session et pourrait être élaguée (facultatif).
N'écris rien dans un AGENTS.md ou CLAUDE.md sans mon accord explicite. Le niveau stack ne s'écrit jamais ici : propose la ligne, je la porte dans le hub.
