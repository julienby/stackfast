# Copier en .env (gitignoré). Une variable par secret, préfixée par le slug de l'app en majuscules.
# Instance
PROXY_NETWORK=caddy-net
HOST_UID=1000
HOST_GID=1000
PHP_HTMX_CONTAINER=php-htmx

# Apps (exemples)
# DEMO_ADMIN_PASSWORD=
# DEMO_API_TOKEN=   # bearer /api + /mcp, généré par bin/new-app
